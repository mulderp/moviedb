<div id="genres-filter">
<p> Genres:</p>
<ul>
  {{#each items }}
    <li>
    <a href="#genre/{{ name }}" class="genre-item" data-genre="{{ name }}"><i class="icon-chevron-right"></i>
    {{ display_name }} ({{ total }})</a>
    </li>
  {{/each}}
</ul>
</div>
